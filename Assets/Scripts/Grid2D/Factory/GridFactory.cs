using System.Collections.Generic;
using System.Linq;

public class GridFactory
{
    // public Grid2D Create(GridConfig config)
    // {
    //     return new Grid2D(config.Id, config.X, config.Y, config.MaxTeamCost, config.PlayerCount);
    // }
    
    // public int[][] GetPositions(List<PositionRangeConfig> configs)
    // {
    //     var positions = new List<List<int>>();
    //     foreach (var config in configs)
    //     {
    //         var list = new List<int>();
    //         list.AddRange(config.Positions);
    //         foreach (var range in config.Ranges)
    //         {
    //             list.AddRange(Enumerable.Range(range.Start, range.End - range.Start + 1));
    //         }
    //         positions.Add(list);
    //     }
    // }
}